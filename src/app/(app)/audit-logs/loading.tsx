import { AppRouteSkeleton } from "~/components/layout/app-route-skeleton";

export default function AuditLogsLoading(): React.JSX.Element {
  return <AppRouteSkeleton label="Loading audit logs" variant="table" />;
}
