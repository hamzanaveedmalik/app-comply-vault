import { AppRouteSkeleton } from "~/components/layout/app-route-skeleton";

export default function IntegrationsLoading(): React.JSX.Element {
  return <AppRouteSkeleton label="Loading integrations" />;
}
