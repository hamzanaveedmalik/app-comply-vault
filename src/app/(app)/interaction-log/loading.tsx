import { AppRouteSkeleton } from "~/components/layout/app-route-skeleton";

export default function InteractionLogLoading(): React.JSX.Element {
  return <AppRouteSkeleton label="Loading interaction log" variant="table" />;
}
