import { AppRouteSkeleton } from "~/components/layout/app-route-skeleton";

export default function ReviewLoading(): React.JSX.Element {
  return <AppRouteSkeleton label="Loading review queue" variant="table" />;
}
